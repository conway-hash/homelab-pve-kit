# Managed by Ansible (svc_linkwarden) — edits will be overwritten.
# Mode 0600: this file holds a DNS-write credential for the whole zone.

CLOUDFLARE_API_TOKEN={{ svc_linkwarden_cloudflare_api_token }}
