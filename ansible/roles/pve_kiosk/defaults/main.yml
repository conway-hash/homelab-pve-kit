---
# Intentionally empty of secrets — this role needs none. Everything it
# serves is read locally off this box and never leaves it: the daemon
# binds 127.0.0.1 only, and the actions it exposes are reachable only from
# the machine itself.
#
# Vars with real defaults live in group_vars/pve_host/vars.yml, not here.
